//document.addEventListener("DOMContentLoaded", function() {
//	const apiResponseDiv = document.getElementById('api-response');
//  
//	fetch('http://localhost/api/ping/')
//	  .then(response => response.json())
//	  .then(data => {
//		console.log(data);
//		apiResponseDiv.textContent = data.message;
//	  })
//	  .catch(error => {
//		console.error('Error:', error);
//		apiResponseDiv.textContent = 'Failed to fetch API response';
//	  });
//  });
// game3D.js

// Placeholder for game logic
console.log("game3D.js is loaded");

// You can define basic functions here if needed later
function initializeGame() {
    console.log("Game initialization logic will go here.");
}

// Call the function to confirm it's working for now
initializeGame();